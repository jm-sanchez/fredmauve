<form method="post" action="{{ path('app_image_news_dashboard_delete', {'id': image_news.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ image_news.id) }}">
    <button class="btn">Delete</button>
</form>
