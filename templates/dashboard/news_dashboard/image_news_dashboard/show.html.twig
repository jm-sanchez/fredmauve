{% extends 'base.html.twig' %}

{% block title %}ImageNews{% endblock %}

{% block body %}
    <h1>ImageNews</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ image_news.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ image_news.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_image_news_dashboard_index') }}">back to list</a>

    <a href="{{ path('app_image_news_dashboard_edit', {'id': image_news.id}) }}">edit</a>

    {{ include('image_news_dashboard/_delete_form.html.twig') }}
{% endblock %}
